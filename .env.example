# .env.example - Example .env configuration file
# Copy this file to .env and modify as needed

# Site identifier, used in exception notification emails
SITE=ficnet-bania

# Path to the application log file
LOG_FILE=/var/www/logs/ficnet-bania.app.log

# PSR-3 log level (e.g. 'debug', 'info', 'notice', etc.)
# See http://www.php-fig.org/psr/psr-3/
LOG_LEVEL=debug

# Email address to send error emails to (leave blank to disable)
ERROR_NOTIFICATION_EMAIL=david.bania@foresters.com

# Whether to send log messages to the browser console via ChromeLogger (1 or 0)
ENABLE_BROWSER_CONSOLE_LOGGING=0

# Whether to display error debugging information in the browser (1 or 0)
ENABLE_DEBUG_ERROR_PAGE=1

# LDAP connection configuration
LDAP_HOST="atlanta.firstinvestors.com harrisburg.firstinvestors.com"
LDAP_SSL=1
LDAP_DOMAIN=firstinvestors.com
LDAP_BASE_DN=DC=firstinvestors,DC=com
LDAP_USERNAME=http-read-only
LDAP_PASSWORD=5TraZrm1

# ADMBILL database connection configuration
DB_ADMBILL_DRIVER=ibm_db2
DB_ADMBILL_HOST=localhost
DB_ADMBILL_PORT=
DB_ADMBILL_DATABASE=admbill
DB_ADMBILL_USERNAME=ficweb
DB_ADMBILL_PASSWORD=kzH34BBM

# ADMIVR database connection configuration
DB_ADMIVR_DRIVER=ibm_db2
DB_ADMIVR_HOST=localhost
DB_ADMIVR_PORT=
DB_ADMIVR_DATABASE=admivr
DB_ADMIVR_USERNAME=db2test
DB_ADMIVR_PASSWORD=bl576a

# BOSS_000 database connection configuration
DB_BOSS_000_DRIVER=ibm_db2
DB_BOSS_000_HOST=localhost
DB_BOSS_000_PORT=
DB_BOSS_000_DATABASE=boss_000
DB_BOSS_000_USERNAME=bossweb
DB_BOSS_000_PASSWORD=89dfr2Pc7T

# BOSS_WEB database connection configuration
DB_BOSS_WEB_DRIVER=ibm_db2
DB_BOSS_WEB_HOST=localhost
DB_BOSS_WEB_PORT=
DB_BOSS_WEB_DATABASE=boss_web
DB_BOSS_WEB_USERNAME=bossweb
DB_BOSS_WEB_PASSWORD=89dfr2Pc7T

# FICWEB database connection configuration
DB_FICWEB_DRIVER=ibm_db2
DB_FICWEB_HOST=localhost
DB_FICWEB_PORT=
DB_FICWEB_DATABASE=ficweb
DB_FICWEB_USERNAME=ficweb2
DB_FICWEB_PASSWORD=tvA67eDd

# FAM database connection configuration
DB_FAM_DRIVER=ibm_db2
DB_FAM_HOST=localhost
DB_FAM_PORT=
DB_FAM_DATABASE=fam
DB_FAM_USERNAME=webuser
DB_FAM_PASSWORD=B79qzEVn

# Path to the application log file
LOG_FILE=/var/www/logs/ficnet-bania.app.log

# PSR-3 log level (e.g. 'debug', 'info', 'notice', etc.)
# See http://www.php-fig.org/psr/psr-3/
LOG_LEVEL=debug

# Path to the database log file (leave empty to disable database logging)
DATABASE_LOG=/var/www/logs/database.log

# GitLab Private token
GITLAB_PRIVATE_TOKEN=9vbB13PfA4gZ5sAsKLuV

# Email address to send Suggestion Box emails to
SUGGESTION_BOX_EMAIL=david.bania@foresters.com

# Comma-delimited list of IP addresses to prevent submission of Suggestion Box emails
SUGGESTION_BOX_BLACKLISTED_IPS=192.168.1.82

# Email address to send all opt-out notifications to (keep blank to send to the
# actual recipients)
EMAIL_OPT_OUT_NOTIFICATION_OVERRIDE_EMAIL=david.bania@foresters.com

# SSO login URI
SSO_LOGIN_URI=https://sso.forestersfinancial.com/sso/v1/login

# SSO token validation and user data retrieval URI
SSO_USER_URI=https://sso.forestersfinancial.com/sso/v1/user
